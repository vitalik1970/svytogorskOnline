name: Deploy to Server

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Save build artifact
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: build

  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Upload S3
        uses: shallwefootball/upload-s3-action@v1.3.3
            
        with:
          aws_key_id: AKIA6ODU6WWC2NOZA25H
          aws_secret_access_key: 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
          aws_bucket: svytogorsk
          source_dir: 'https://github.com/vitalik1970/svytogorskOnline/actions/runs/8030919926/artifacts/1271995589'
